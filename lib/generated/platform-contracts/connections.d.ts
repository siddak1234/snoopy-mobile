/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/v1/connections/providers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The providers this deployment can actually authorize.
         * @description An **OAuth** provider appears here only when it has BOTH a reviewed spec file and client credentials in this deployment's configuration, so a workspace is never shown a Connect button that cannot work.
         *     An **api-key** provider needs no deployment credential and is always offered: the customer supplies the credential, so there is nothing for an operator to register and nothing that could be missing.
         *     Not workspace-scoped: the answer is a property of the deployment, not of a tenant. A session is still required — the list names which vendors this platform integrates with, which is not public information.
         */
        get: operations["listConnectionProviders"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/connections/callback": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The address a provider redirects the browser back to.
         * @description Reached by a top-level browser navigation from the provider, never by a client calling it directly. It always answers 302 — its caller is a person mid-flow, not a program, so every outcome ends at the web app carrying a short reason token rather than a JSON body nobody will see. That includes an unresolvable session (`reason=session_expired`) and an unconfigured connections hop (`reason=not_configured`).
         *     A session is required despite the redirect, which is why this operation does not clear `security`: the actor completing the flow is compared against the one who began it, and an unauthenticated callback has no actor to compare.
         *     This route is not protected by same-origin: a cross-site GET navigation carries no `Origin` header. It is protected by the authorization attempt itself. The `state` is single-use and claimed by a conditional update, so a replayed callback finds nothing to claim, and the service refuses a callback whose actor is not the person who began the flow — without which anyone able to start an attempt could complete it with their own provider account and have every automation in the workspace run through it.
         */
        get: operations["completeConnectionAuthorization"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/workspaces/{workspaceId}/connections": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The accounts this workspace has connected. */
        get: operations["listConnections"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/workspaces/{workspaceId}/connections/key": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Connect a provider whose customers paste a credential.
         * @description **Requires `owner` or `admin`**, for the same reason `authorize` does: one live connection per (workspace, provider), and connecting supersedes the previous one, so a member could otherwise replace the organization's account with their own.
         *     For providers whose `authType` is `api-key` (BUILD-PLAN 5.14, ADR-0015). An OAuth provider sent here is refused with 400 rather than quietly redirected — the caller believes it holds a credential, and the honest reply is that this provider does not take one.
         *     **The credential is verified before it is stored.** The reviewed spec names a cheap read operation, which is called immediately; a credential the vendor rejects yields 400 and no connection. Without that, a customer sees "Connected" and their automation breaks days later with a vendor error nobody can read. The vendor's own rejection body is deliberately NOT relayed: vendors quote the offending credential back, and that would put a customer's secret into a response and then into a log.
         *     **Idempotent per `Idempotency-Key`.** A retry with the same key and credential input returns the original connection without repeating vendor verification or superseding it again; reusing a key with different input, or retrying while the first request is still processing, returns 409.
         *     `credentials` keys are the `credentialFields[].name` values the provider list reports for this provider. Every declared field is required; an undeclared one is refused by name rather than ignored, so a caller sending `apikey` for a spec declaring `apiKey` is told which it was.
         */
        post: operations["connectProviderWithKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/connections/native/complete": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * A native client trades its sealed callback for a connection.
         * @description The second half of the split callback (BUILD-PLAN 7.7.6, §12.1 #62).
         *     A native client's system browser holds no platform session, so `/v1/connections/callback` cannot complete its flow — it has no actor to compare against the person who began the attempt. Instead the callback seals the provider's parameters and redirects to the app's claimed HTTPS URL, and the app posts the sealed value here **with its bearer token**.
         *     **This is where the flow finally has an actor, so this is where the attempt is claimed** — for the first time, exactly once. A replayed `code` therefore fails twice over: the attempt is no longer claimable, and the provider's authorization code inside it is spent (RFC 6749 §4.1.2).
         *     Answers JSON rather than a redirect: the caller here is the app on a POST, not a browser mid-navigation. Every unusable code — forged, tampered, expired, or naming a consumed attempt — is one 401 with no detail that would tell them apart.
         */
        post: operations["completeNativeConnectionAuthorization"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/workspaces/{workspaceId}/connections/authorize": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Begin connecting a provider account.
         * @description **Requires `owner` or `admin`.** Completing this flow supersedes any live connection for the same provider, so it is not an additive action a plain member may take. A member receives 403 with `details.requiredRole` — 403 rather than 404, since they already know the workspace exists.
         *     Returns where to send the browser. `actorUserId` is not accepted — the actor comes from the session, and the same person must complete the flow.
         *     The single-use `state` is deliberately NOT returned. It is already inside the authorization URL, and handing the browser a second copy of it creates somewhere it can be logged or referred onward without the flow gaining anything.
         *     `scopes` may narrow the request to a subset of what the reviewed spec declares. It can never widen it: a scope the spec does not declare is refused, so a caller cannot have the platform ask a provider for permissions nobody reviewed.
         */
        post: operations["beginConnectionAuthorization"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/workspaces/{workspaceId}/connections/{connectionId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Disconnect an account.
         * @description **Requires `owner` or `admin`.** Disconnecting breaks every automation in the workspace that depends on the provider, which is not a plain member's decision to make.
         *     Local token material is deleted by a schema trigger on the status change, and revocation upstream is attempted afterwards. Deliberately in that order: a provider that is down must never leave a customer unable to disconnect. A revocation that did not land is logged, and the grant then lives at the provider until its own expiry.
         */
        delete: operations["disconnectConnection"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        Problem: {
            type: string;
            title: string;
            status: number;
            code: string;
            detail?: string;
            requestId?: string;
            details?: {
                [key: string]: unknown;
            };
        };
        ConnectionProvider: {
            providerId: string;
            displayName: string;
            /** @description What connecting this account lets automations do, in a person's words. */
            description: string;
            /**
             * @description An icon name the clients resolve. Never an asset URL — the same rule and the same vocabulary as `AutomationCatalogEntry.icon`.
             *     Required, so a reviewed provider file stays the whole of what adding a provider takes. Without it a client has to keep its own map from `providerId` to an icon, and a new vendor stops being a data-file change.
             */
            icon: string;
            /** @description Every scope the reviewed spec declares. An authorization may request a subset of these and never anything outside them. Empty for an `api-key` provider — scopes are granted, not pasted. */
            scopes: string[];
            /**
             * @description Which door this provider connects through: `oauth2` sends the browser to the vendor's consent page, `api-key` collects pasted values (BUILD-PLAN 5.14, ADR-0015). A client renders the right form from this rather than assuming every provider redirects.
             * @enum {string}
             */
            authType: "oauth2" | "api-key";
            /** @description Present only when `authType` is `api-key`. **The form is generated from this**, the way an automation's setup form is generated from `manifest.setup[]` — so a new key-based vendor is a reviewed file and no client change. */
            credentialFields?: {
                /** @description The key to send under `credentials`, and what the spec's template interpolates. */
                name: string;
                label: string;
                /** @description Whether this value is a credential rather than an identifier. Stated by the spec, never inferred: Twilio's Account SID is public and its Auth Token is not, and a field wrongly assumed public is one a client may render back to the screen. */
                secret: boolean;
                /** @description Where the customer finds this value in the vendor's console. */
                help?: string;
            }[];
        };
        /**
         * @description The values `ConnectionStatus` in `packages/contracts` declares and the schema's own CHECK constraint permits. `not-connected` is in the type but never on a row — it describes a provider with no connection at all, which this list simply omits.
         * @enum {string}
         */
        ConnectionStatus: "authorizing" | "connected" | "reauthorization-required" | "disconnected";
        /** @description A connection as the Connections service knows it. Every field here comes from that one service. Used on its own where no Catalog read is made — the disconnect response, which reports the state change and nothing that would require a second hop to compute. */
        ConnectionState: {
            /** Format: uuid */
            id: string;
            providerId: string;
            /** Format: uuid */
            workspaceId: string;
            /** @description Which account at the provider this is — the provider's own identifier and label, read from the token response at the path the spec names. It is how a person tells two connected accounts apart. */
            externalAccount: {
                id: string;
                displayName: string;
            };
            status: components["schemas"]["ConnectionStatus"];
            requiredScopes: string[];
            /** @description As granted, which a provider may narrow from what was requested. Worth rendering: an automation can fail on a scope the person declined. */
            grantedScopes: string[];
            /** Format: date-time */
            lastValidatedAt?: string;
            /** @description Why a connection is not usable — set when the refresh loop retires one it cannot renew. Never a provider's raw error text. */
            errorCode?: string;
        };
        /** @description A connection plus what depends on it — the shape the list read returns. `usedByCount` is the one field no single service can answer, so the Edge composes it from a Catalog read alongside the Connections read. */
        Connection: components["schemas"]["ConnectionState"] & {
            /** @description How many **live** subscriptions in this workspace require this provider — what breaks if the connection is removed. A draft subscription is not counted: it is being set up, not running. */
            usedByCount: number;
        };
    };
    responses: {
        /** @description RFC 9457 problem document. */
        Problem: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
    };
    parameters: {
        /** @description The workspace being authorized. Membership is checked at the Edge; a workspace the caller does not belong to answers 404. */
        WorkspaceId: string;
        /** @description 16-128 characters of letters, numbers, dot, underscore, tilde, colon, or hyphen. The same key with different input is a conflict, not a replay. */
        IdempotencyKey: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    listConnectionProviders: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every provider on offer, in registry order. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        providers: components["schemas"]["ConnectionProvider"][];
                    };
                };
            };
            401: components["responses"]["Problem"];
            503: components["responses"]["Problem"];
        };
    };
    completeConnectionAuthorization: {
        parameters: {
            query: {
                /** @description The single-use value minted when the authorization began. */
                state: string;
                /** @description The authorization code, when the person consented. */
                code?: string;
                /** @description The provider's refusal code, when they did not. */
                error?: string;
                /** @description The issuer, when the provider echoes one. Checked against the provider the attempt was started with — mix-up defence, RFC 9700 §4.4. */
                iss?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Redirect to the web app's connections page. `status=connected` with the `provider` on success; `status=error` with a `reason` token otherwise. The destination is a fixed path with fixed parameters — never anything the provider supplied. */
            302: {
                headers: {
                    location?: string;
                    [name: string]: unknown;
                };
                content?: never;
            };
            503: components["responses"]["Problem"];
        };
    };
    listConnections: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The workspace being authorized. Membership is checked at the Edge; a workspace the caller does not belong to answers 404. */
                workspaceId: components["parameters"]["WorkspaceId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every connection the workspace holds, live or broken. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        connections: components["schemas"]["Connection"][];
                    };
                };
            };
            401: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            503: components["responses"]["Problem"];
        };
    };
    connectProviderWithKey: {
        parameters: {
            query?: never;
            header: {
                /** @description 16-128 characters of letters, numbers, dot, underscore, tilde, colon, or hyphen. The same key with different input is a conflict, not a replay. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description The workspace being authorized. Membership is checked at the Edge; a workspace the caller does not belong to answers 404. */
                workspaceId: components["parameters"]["WorkspaceId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    providerId: string;
                    /** @description Field names come from the provider's own spec, so this object is deliberately open — the Edge relays it without knowing which fields a vendor declares. Each value is a non-empty string of at most 4096 characters, trimmed before use, because a pasted value carries whatever whitespace the clipboard did. The Edge accepts up to 72 KiB so even four maximum four-byte UTF-8 values plus JSON framing fit this contract. */
                    credentials: {
                        [key: string]: string;
                    };
                };
            };
        };
        responses: {
            /** @description The credential was accepted by the provider and stored. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        connection: components["schemas"]["Connection"];
                    };
                };
            };
            400: components["responses"]["Problem"];
            401: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            /** @description The key was reused with different credentials, or its first request is still verifying. Retry the same key after the in-progress request resolves; do not create a new key for an uncertain request. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The provider could not be reached to verify the credential, or its spec cannot build a verification call as written. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The connections adapter is unconfigured, or the provider is rate limiting or down. A vendor that is unavailable has not rejected anything, so this is deliberately not a 400 — telling a customer their key is wrong when the vendor is down sends them to rotate a credential that was fine. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    completeNativeConnectionAuthorization: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description The sealed value the callback redirect carried. Opaque, and bound to a key the app never holds — it carries the provider's authorization code, never a provider token. */
                    code: string;
                };
            };
        };
        responses: {
            /** @description The connection, as the list read returns it. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        connection: components["schemas"]["Connection"];
                    };
                };
            };
            400: components["responses"]["Problem"];
            401: components["responses"]["Problem"];
            503: components["responses"]["Problem"];
        };
    };
    beginConnectionAuthorization: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The workspace being authorized. Membership is checked at the Edge; a workspace the caller does not belong to answers 404. */
                workspaceId: components["parameters"]["WorkspaceId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    providerId: string;
                    /** @description Bounded so the advertised maximum fits inside the route's 8 KiB body limit — a documented maximum the server refuses is worse than a smaller one it honours. Real provider scopes are far below this: Google's longest is around 50 characters. */
                    scopes?: string[];
                    /**
                     * Format: uri
                     * @description **Native clients only** (BUILD-PLAN 7.7.6). An app-claimed HTTPS URL from `NATIVE_APP_REDIRECT_URIS`, exact-matched. Its presence is what makes this a native flow: the callback then hands the app a sealed value to complete instead of finishing at the website.
                     *     Omit it for the browser flow, which is unchanged. A value not on the allowlist is refused with 400 and is **not echoed back** — reflecting a rejected redirect target is how a refusal becomes a reflection gadget.
                     */
                    returnTo?: string;
                };
            };
        };
        responses: {
            /** @description Where to send the browser, and when the attempt expires. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /**
                         * Format: uri
                         * @description The provider's own consent page, with PKCE and state applied.
                         */
                        authorizationUrl: string;
                        /**
                         * Format: date-time
                         * @description After this, the attempt can no longer be completed.
                         */
                        expiresAt: string;
                    };
                };
            };
            400: components["responses"]["Problem"];
            401: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            503: components["responses"]["Problem"];
        };
    };
    disconnectConnection: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The workspace being authorized. Membership is checked at the Edge; a workspace the caller does not belong to answers 404. */
                workspaceId: components["parameters"]["WorkspaceId"];
                connectionId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The connection, now disconnected. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        connection: components["schemas"]["ConnectionState"];
                    };
                };
            };
            401: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            503: components["responses"]["Problem"];
        };
    };
}
